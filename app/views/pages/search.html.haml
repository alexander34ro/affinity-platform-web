.components-container.py-3
  - @components.each do |component|
    .component.card.mb-3.p-3{ data: { config_options: (component[:config_options] rescue []), default: (component[:default] rescue []) } }
      %h5.card-title= component[:name] rescue component
      %h6.card-subtitle.text-muted= component[:description] rescue ''
